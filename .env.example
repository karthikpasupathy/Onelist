# Copy to .env.local for local development
# These values should be set in Vercel environment variables for production

# InstantDB App ID (public - injected into frontend)
VITE_INSTANT_APP_ID=

# OpenAI API Key (server-side only - used by /api/openai)
OPENAI_API_KEY=
